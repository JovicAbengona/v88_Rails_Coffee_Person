// Generated by CoffeeScript 1.12.7
(function() {
  var Person, person;

  Person = (function() {
    function Person(name) {
      this.name = name;
      this.distance_travelled = 0;
    }

    Person.prototype.say_name = function() {
      return console.log(this.name);
    };

    Person.prototype.say_something = function(something) {
      if (something == null) {
        something = "something";
      }
      return console.log(this.name + " says " + something);
    };

    Person.prototype.crawl = function() {
      this.distance_travelled += 1;
      console.log(this.name + " is crawling...");
      return console.log("Distance Travelled: " + this.distance_travelled);
    };

    Person.prototype.walk = function() {
      this.distance_travelled += 3;
      console.log(this.name + " is walking...");
      return console.log("Distance Travelled: " + this.distance_travelled);
    };

    Person.prototype.run = function() {
      this.distance_travelled += 10;
      console.log(this.name + " is running...");
      return console.log("Distance Travelled: " + this.distance_travelled);
    };

    return Person;

  })();

  person = new Person("Trey");

  person.say_name();

  person.say_something("Hello!");

  person.crawl();

  person.walk();

  person.run();

}).call(this);
